name: Build

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout latest code
        uses: actions/checkout@v2

      # Note: jdk8 is necessary to use javah
      - name: Set up JDK 8
        uses: actions/setup-java@v1
        with:
          java-version: 8

      - name: Set up build cache
        uses: actions/cache@v1
        with:
          path: ~/.gradle/caches
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle.kts') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Build shared libraries using Docker
        run: |
          make headers libs-docker res

      - name: Install solver libs
        run: |
          install -m 644 build/lib/libminisat.so /usr/lib
          install -m 644 build/lib/libcadical.so /usr/lib

      - name: Build package using Gradle wrapper
        run: ./gradlew build

      - name: Upload to GH Releases
        uses: softprops/action-gh-release@v0.1.5
        if: startsWith(github.ref, 'refs/tags/')
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          files: |
            build/lib/libjminisat.so
            build/lib/libjcadical.so
            build/lib/libminisat.so
            build/lib/libcadical.so

name: Build (Windows)

on: [push, pull_request]

jobs:
  build:
    runs-on: windows-latest
    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout latest code
        uses: actions/checkout@v2

      # Note: jdk8 is necessary to use javah
      - name: Set up JDK 8
        uses: actions/setup-java@v1
        with:
          java-version: 8

      - name: Set up build cache
        uses: actions/cache@v2
        with:
          path: $HOME/.gradle/caches
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Build minisat.dll
        working-directory: kotlin-satlib-jni
        run: |
          git clone https://github.com/niklasso/minisat minisat-src
          cd minisat-src
          git apply -v ../patches/minisat-*.patch
          cmake -B build
          cmake --build build --target minisat-lib-shared

      - name: Compile classes
        run: ./gradlew classes --stacktrace

      - name: Build jminisat.dll
        working-directory: kotlin-satlib-jni
        run: |
          javah -d build/headers -classpath build/classes/kotlin/main com.github.lipen.satlib.solver.jni.JMiniSat
          mkdir -p build/lib
          g++ -Wall -O3 -fPIC -shared -s -D__STDC_LIMIT_MACROS -D__STDC_FORMAT_MACROS -I${JAVA_HOME}/include -I${JAVA_HOME}/include/win32 -Ibuild/headers -Iminisat-src -Lminisat-src/build src/main/cpp/JMiniSat.cpp -lminisat -lz -o build/lib/jminisat.dll
          install -m 644 build/lib/jminisat.dll -Dt src/main/resources/lib/win64

      - name: Build package using Gradle wrapper
        run: ./gradlew build -x test --stacktrace

      - name: Upload to GH Releases
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          files: |
            build/lib/jminisat.dll
            kotlin-satlib-jni/minisat-src/build/minisat.dll
